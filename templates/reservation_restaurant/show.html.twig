{% extends 'base.html.twig' %}

{% block title %}ReservationRestaurant{% endblock %}

{% block body %}
    <h1>ReservationRestaurant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_restaurant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ reservation_restaurant.nom }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ reservation_restaurant.tel }}</td>
            </tr>
            <tr>
                <th>Nombreplace</th>
                <td>{{ reservation_restaurant.nombreplace }}</td>
            </tr>
            <tr>
                <th>Reservationdate</th>
                <td>{{ reservation_restaurant.reservationdate ? reservation_restaurant.reservationdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_restaurant_index') }}">back to list</a>

    <a href="{{ path('reservation_restaurant_edit', {'id': reservation_restaurant.id}) }}">edit</a>

    {{ include('reservation_restaurant/_delete_form.html.twig') }}
{% endblock %}
